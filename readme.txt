If the search input is entered in the terminal instead of the input field in `rofi`, 
press `Enter` so that your search keyword appears as a list item in `rofi`. 
Then, click on the list item to send the request to the API.